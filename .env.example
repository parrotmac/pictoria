# Storage configuration
# Options: file (default), postgres
STORAGE_TYPE=postgres

# PostgreSQL connection string (required when STORAGE_TYPE=postgres)
DATABASE_URL=postgres://pictoria:pictoria_secret@localhost:5432/pictoria?sslmode=disable

# Server port (optional, defaults to 8080)
PORT=8080